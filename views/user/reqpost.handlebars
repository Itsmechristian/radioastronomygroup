  <div class="container b-reqpost-container">
  <div class="b-reqpost">
  <h1>{{post.title}}</h1>
  <p>{{{post.body}}}</p>
  </div>
  <form action="/user/request/post/{{post._id}}" method="POST">
    <input type="hidden" name="_csrf" value="{{ csrfToken }}">
    <button type="submit" id="btn">Post</button>
  </form>
</div>